---
title: 모니터 뷰어 — 실시간 보드
status: todo
ordinal: 8000
created: 2026-09-16
---

## Goal
<!-- kanban:goal:begin -->
읽기 전용 localhost 웹 뷰어 llm-wiki monitor로 카드 집김·진행 중·활동 로그를 실시간 표시한다 — 쓰기 경로는 없고 카드를 쓰는 건 CLI뿐이다
<!-- kanban:goal:end -->

## Acceptance Criteria
<!-- kanban:ac:begin -->
- [ ] #1 llm-wiki monitor가 127.0.0.1에서 Node 내장 모듈만으로 HTTP 서버를 띄운다(의존성 0 추가)
- [ ] #2 GET /api/board가 보드 뷰를, GET /api/activity가 최근 활동을 내놓고 페이지가 폴링으로 갱신한다
- [ ] #3 DOING 카드에 클레임 주체·경과 시간·만료 경고가 뜨고 REVIEW 질문·TODO 게이트·의존 대기가 표시된다
- [ ] #4 쓰기 메서드(POST 등)는 전부 거부된다
<!-- kanban:ac:end -->

## Plan

## Notes
- 2026-09-16T12:35-07:00 — 설계 리뷰(2026-09-16 subagent) 반영: ① viewCard에 claimedAt 추가는 카드 A 선행 후(JSON 스키마가 모니터 사유화되는 시점). ② 표면 누락 방지: printUsage 행·KNOWN_SUBCOMMANDS·USAGE.monitor·package.json scripts.test 등록(목록 밖 테스트 = 조용한 no-op). ③ monitor는 auto-update 트리거 제외 — stdout 첫 줄이 URL 계약(wait 전례). ④ --port 0 가용 + 첫 줄에 http://127.0.0.1:<port> 출력(테스트가 파싱), EADDRINUSE는 error 이벤트에서 exit 1. ⑤ 페이지 DOM은 textContent만(제목·질문은 에이전트 작성 — 인젝션). ⑥ 시작 시 보드 루트 검증, 요청 오류는 500으로 서버 생존. ⑦ plan.md [확정] 순서 블록에 읽기 전용 층 분리 일지 추가.

## Handoff

## Result
